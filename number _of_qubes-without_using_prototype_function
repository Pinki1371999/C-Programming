#include <iostream>
using namespace std;
int sum(int a)
{
    return(a*a*a);
}
int main()
{
    int a,b;
    cout<<"enter a number";
    cin>>a;
    b=sum(a);
    //cout<<sum(a);
    cout<<b;
   return 0;
}
