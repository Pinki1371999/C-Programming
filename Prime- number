#include<iostream>
using namespace std;
int main()
{  
    int flag=0,num,d=3;
    
    cout<<"Enter a number = ";
    cin>>num;
    
    if(num==0 || num==1)
    {
        flag=1;
    }
    
    else
    {
        while(num>d)
        {   
            if(num%d==0)
            {
                flag=1;
            }
            d+=1;
        }
    }
    
    if(flag==0)
    {
        cout<<"prime";
    }
    else
    {
        cout<<"Not prime";
    }
    
    return 0;
}
